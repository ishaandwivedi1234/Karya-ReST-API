const express = require("express");
// const users = require("../models/user");
// const auth = require("../autha/auth");
// module.exports = function (app) {
//   app.use(express.json());
//   app.use("/api/users", users);
//   app.use("/api/", auth);
//   //   app.use(error());
// };
